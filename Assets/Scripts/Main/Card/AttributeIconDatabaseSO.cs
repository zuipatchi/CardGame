namespace Main.Card
{
    // AttributeDatabaseSO に統合済み。このクラスは型エイリアスとして残す（Unity アセット参照維持のため）。
    // Inspector でアセットを再アサインしたら削除可。
    public sealed class AttributeIconDatabaseSO : AttributeDatabaseSO { }
}
