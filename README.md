# Card
マルチプレイヤーカードゲーム
ここには開発者向けのメモを記載する

## 開発の進め方
1. `/feature` を実行して新機能を実装する（ヒアリング→実装→テストまで自動で進む）
2. PlayMode: Window → General → Test Runner → PlayMode タブ → Run All で自動テストを実行する
3. Unity Editor で Play して動作確認する
4. 問題なければ `/ship` を実行してコミット・ドキュメント更新まで行う

## テストプレイの仕方
決まったら記載する

## 機能一覧
- BGMとSEの再生機能
- シーン管理機能（Commonシーンをベースに他のシーンを使用する）
  - フェードイン/アウトによる画面遷移演出
- オプション機能（BGM音量・SE音量・タイトルに戻る）
- オンラインマッチング機能（UGS Multiplayer Services）
  - クイックマッチ（空きルームへ自動参加 or 作成して待機）
  - ルーム一覧から手動参加
  - 20秒タイムアウト＋リトライ確認
- デッキ構築（Title → DeckBuilder → Main の遷移）
  - カード一覧をキャラ・技・イベントのセクション別に表示。グリッドから選択・枚数管理（最小5枚・最大20枚）・背景画像（Addressables）
  - デッキを PlayerPrefs に JSON 保存・次回起動時に復元
- カードゲーム盤面（Main シーン）
  - カードテンプレート（UXML）+ カードデータベース（ScriptableObject）、カードにイラスト（Sprite）設定可能
  - 盤面背景画像（Addressables）
  - 手札：初期5枚をシャッフル後に扇状表示（画面下端・60%スケール）、ホバーで1.5倍拡大＋最前面表示、ドロップ後に残りカードが詰まるアニメーション
  - 相手手札：画面上部に裏向き・反転表示（非インタラクティブ）
  - フィールド：自分フィールド・相手フィールドの2エリア、各最大5枚を中央寄せ表示、ドラッグ&ドロップで手札から配置
  - デッキ：自分（右下）・相手（左上・反転）それぞれ裏向きで積み重ね表示
  - カード裏返しアニメーション（DOTween）
  - ドロー演出：デッキ位置から手札へ飛翔→フリップ（自分・相手ともに初期5枚を0.12秒ずつずらして並走。相手は裏向きのまま）
  - カード情報表示：カード名・コストバッジ・攻撃力・防御力を縦一列左寄せで表示
  - 墓地ゾーン：プレイヤー墓地（左下）・相手墓地（右上）を常時表示。GraveIcon で枚数を表示
  - 攻撃矢印：フィールドカードをドラッグするとベジェ曲線の矢印を表示
  - OK/戻るボタン：画像ボタン（右下横並び）でカード配置・攻撃操作をステージング
  - ゲームロジック（ターンフロー）
    - マリガン（初期手札にキャラなし → 手札返却・リシャッフル・再ドロー。プレイヤー・CPU それぞれ1回）
    - キャラセットフェーズ（ゲーム開始時1回のみ・先攻から順に裏向き配置・Resolve で同時表向き）
    - ターン管理：Draw → 戦闘前1 → 戦闘前2 → 解決 → 戦闘
    - 戦闘前1：キャラまたは技カードを1枚だけ裏向きでフィールドにセット
    - 戦闘前2：イベントカードのみ Ready 化。2連続パスで終了
    - 解決フェーズ：Ready カードを逆順で処理・AtkBoost / DefBoost 効果を適用
    - 戦闘フェーズ：全フィールドカードを表向き→キャラスロットへ移動→技カードでダメージ計算（ATK + AtkBoost − (DEF + DefBoost)）
    - イベントカード効果（AtkBoost: 次の戦闘フェーズの ATK を加算 / DefBoost: 次の戦闘フェーズの有効 DEF を加算）
    - デッキ攻撃（ダメージ分だけ相手デッキを墓地へ）・勝利条件（デッキ0枚）
    - 勝敗判定（ドローフェーズ末尾・戦闘フェーズ末尾に実施）
  - Resolve テキスト演出・攻撃アニメーション（DOTween）
  - ターン告知演出（「YOUR TURN」水色 / 「ENEMY TURN」赤）
  - フェーズ告知演出（SET CHARACTERS / SET CARDS / SET EVENTS / FIGHT）
  - ターン開始ドロー（告知アニメーションと並走）
  - ダメージ計算式表示（ATK − DEF = ダメージ）
  - CPU 対戦：各フェーズで最適カードを自動選択

## 使用 Package
- Addressables
- R3
- UniTask
- VContainer
- DoTween
- UnityGamingService
- Live2D Cubism SDK（`Assets/Live2D/`、Git 管理対象）

## プラットフォーム
決まったら記載する

## 日本語フォント
- uxmlで日本語フォントを使用したい場合は /Assets/Font に入っている日本語フォントを使用する

## gitignore
- Asset Storeからダウンロードした物は AssetStore ディレクトリに入れるとGitに管理されない
