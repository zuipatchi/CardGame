# ゲームルール

## デッキ構築ルール

### コスト制限

| 項目 | 制限 |
|---|---|
| 推奨コスト | ぴったり 50（`DeckModel.TargetCost`）。超過してもカードを追加できる |
| コストオーバー | 合計が 50 を超えると「コストオーバー」が赤字表示され、コスト数字も赤くなる。Home シーンで対戦（CPU）・オンラインボタンを押すとトーストを表示して遷移をブロックする |
| 同名カードの枚数制限 | なし（同じカードを何枚でも入れられる） |

### 構築手順

1. Home シーンから「デッキ構築」を押すとデッキ構築シーンへ遷移する
2. 左パネルのカード一覧から選択してデッキに追加する（コスト合計は常に表示される）
3. コスト合計が 50 を超えると「コストオーバー」が赤字で表示される
4. 「保存」ボタン（デッキ枚数 > 0 のとき有効）でデッキを保存する
5. 「もどる」を押すと Home シーンに戻る

### その他

- デッキは DeckModel に保持され、DeckBuilder → Main 遷移をまたいで引き継がれる
- CPU デッキは `CpuDeckSO`（ScriptableObject・Addressables キー `"Card/CpuDeck"`）から読み込む
  - カードIDリストが 1 件以上の場合：`CardDatabase.BuildDeck` を呼び出したカード群をシャッフルして使用する
  - カードIDリストが空の場合：カードDB全カードをシャッフルした全枚数をフォールバックとして使用する
- CPU デッキの編集は Unity Editor 専用の `CpuDeckBuilder` シーンで行う（タイトル画面の「CPU デッキ編集」ボタン、`#if UNITY_EDITOR` でのみ表示）。編集内容は `CpuDeckSO`（`Assets/Data/CpuDeck.asset`）に保存され、ビルドに含まれる

---

## ゲーム開始時フロー（1回のみ）

```
マリガンチェック（プレイヤー）
  ↓ 初期手札にキャラカードがなければ「マリガン」告知 → 手札を全てデッキへ返却 → リシャッフル → 再ドロー
  ↓ （1回限り。再ドロー後にもキャラがなくても続行）
マリガンチェック（CPU / 相手プレイヤー）
  ↓ 同上の処理を CPU 側でも実施（オンライン時は相手のマリガンをアニメーション演出で表示）
  ↓
キャラセットフェーズ
  ↓ 両プレイヤーが同時にキャラカードを1枚裏向きでスロットに置く（なければパス可）
  ↓ 2人とも配置または Pass したら Resolve
  ↓ 両スロットのキャラカードを同時に表向きにする
  ↓
コイントス（先攻後攻決定）
  ↓ CPU戦: Random.value > 0.5f でランダムに先攻後攻を決定する
  ↓ オンライン: OnlineInitialState.IsLocalFirst で決定済みの値を使用
  ↓ コインが回転しながら減速・停止（表=先攻、裏=後攻）
  ↓ 0.5秒後に「先攻」または「後攻」を表示
  ↓
通常ターンループへ
```

**オンライン時のマリガン**: ホストがデッキ配布時に両プレイヤーの手札を確認してマリガン要否を判定し、`NGS_InitialState` に `localNeedsMulligan` / `opponentNeedsMulligan` フラグとして含める。各クライアントは受信したフラグに基づき、自分のマリガンを実行し、相手がマリガンする場合はアニメーション演出を表示する。

---

## ターンフロー

```
ドローフェーズ
  ↓ ターンプレイヤーがデッキから1枚ドロー
  ↓
戦闘前1フェーズ
  ↓ 先攻から順にキャラまたはスキルカードを1枚だけ裏向きでフィールドにセット（パス可）
  ↓
戦闘前2フェーズ
  ↓ ターンプレイヤーと相手が交互にイベントカードを手札からReadyにする
  ↓ 2連続パスで終了
  ↓
解決フェーズ
  ↓ Readyになった順の逆順でイベントカードを処理する（チェーン処理）
  ↓
戦闘フェーズ
  ↓ 全フィールドカードを表向き
  ↓ キャラカード → スロットへ移動（DEF適用）
  ↓ ATK/DEFカウントアップ演出（フィールドにATKアイコン・キャラスロットにDEFアイコン）
  ↓ ボーナスラベル演出（スキルタイプ一致・弱点を突いた・効果がない を ATK数値の右側に表示）
  ↓ スキルカード → 相手キャラスロットへ突撃（両者並行）→ スキルカードは墓地へ
  ↓
相手のターンへ
```

---

## キャラセットフェーズの詳細

1. ゲーム開始時に**1回だけ**実行される（先攻後攻決定より前）
2. **両プレイヤーが同時**にキャラカードを1枚**裏向き**で自スロットに置く（スキップ可）
   - CPU戦: `UniTask.WhenAll` で自分と CPU の選択を並列実行
   - オンライン: 相手カード受信タスクを並列起動し、自分の入力・送信後に相手受信完了を await
3. 両者の配置が完了したら Resolve: 両スロットのキャラカードを**同時に表向き**にする
4. Resolve 後、先攻後攻を決定してコイントス演出を再生し、通常ターンループが開始する

---

## 戦闘前1フェーズの詳細

1. 先攻プレイヤーから順にキャラカードまたはスキルカードを1枚**裏向き**で自フィールドにセットする（スキップ可）
2. 後攻プレイヤーも同様にセットする（スキップ可）
3. セットしたカードは戦闘フェーズまで裏向きのまま

---

## 戦闘前2フェーズの詳細

1. ターンプレイヤーがイベントカードを手札から1枚 Ready 状態にする（スキップ可）
2. 相手プレイヤーがイベントカードを手札から1枚 Ready 状態にできる（スキップ可）
3. 相手が Ready にした場合、ターンプレイヤーが再び Ready の権利を得る
4. **2連続パス**したら戦闘前2フェーズ終了

---

## 解決フェーズの詳細

- Ready になった順の**逆順**でイベントカードを処理する（遊戯王のチェーンに相当）
- カードの対象は Ready になった時点で決定済み
- 処理後、全イベントカードは墓地へ送られる

### イベント効果一覧

| EventType | 効果 | EventValue の意味 |
|---|---|---|
| AtkBoost | 次の戦闘フェーズの ATK に加算 | 加算値 |
| DefBoost | 次の戦闘フェーズの有効 DEF に加算 | 加算値 |
| Draw | 発動プレイヤーが指定枚数デッキ上から手札に加える。1枚引くごとにデッキが 0 になるか確認し、0 になった時点でゲームオーバー判定・残りのドローを中断 | ドロー枚数 |
| Negate | 解決フェーズで自分の直後に処理されるカードの効果を無効化する。無効化されたカード自体は墓地へ送られる。Negate が Negate の直後に処理されると、その Negate を無効化する（連鎖打ち消し） | 使用しない（0 固定） |
| BanishChar | 相手のキャラスロットにいるキャラカードを相手の墓地へ送る。スロットが空の場合は空振り | 使用しない（0 固定） |

---

## 戦闘フェーズの詳細

1. 全フィールドカードを**同時に表向き**にする
2. ターンプレイヤーの「PAY X COSTS」アナウンス → コスト払い → 相手の「PAY X COSTS」アナウンス → コスト払い（コスト 0 の場合も PAY 0 COSTS を表示）
3. キャラカードがフィールドにある場合、**自スロットへ移動**する（既存キャラは墓地へ）
4. ATK/DEFカウントアップ演出を表示する（フィールドにATKアイコン＋ATK値、キャラスロットにDEFアイコン＋DEF値）
5. ボーナスラベル演出を表示する（ATK数値の右側にスライドイン・両者同時表示）
   - タイプ一致（スキル属性==自キャラ属性）→ オレンジ「スキルタイプ一致」
   - 弱点ヒット（スキル属性==相手キャラ弱点）→ ゴールド「弱点を突いた」
   - 得意属性耐性（スキル属性==相手キャラ得意属性）→ グレー「効果がない」（タイプ一致・弱点ラベルより優先）
   - タイプ一致＋弱点が同時の場合は縦並びで2枚表示
6. スキルカードがフィールドから**相手キャラスロットへ突撃**する（予備動作 → 直線飛翔 → ノックバック。両プレイヤー並行）
6. ダメージ計算を行い、相手デッキからカードを墓地へ送る
7. スキルカードを墓地へ送る

### ATK の計算

- ATK = **(スキルダメージ合計 + AtkBoost) × タイプ倍率 × 弱点倍率**
  - タイプ倍率 = **2**：スキルカードの属性 == 自スロットのキャラ属性（`None` 以外）
  - タイプ倍率 = **1**：それ以外（属性不一致・どちらかが `None`）
  - 弱点倍率 = **3**：スキルカードの属性 == 相手キャラの弱点属性（`AttributeDatabaseSO` で設定。`None` 以外）
  - 弱点倍率 = **1**：それ以外
  - AtkBoost はタイプ倍率・弱点倍率の**適用前**に加算する
  - 最大倍率：タイプ一致 ×2 × 弱点 ×3 = **×6**
- 以下の場合は ATK = 0（突撃モーションなし）:
  - キャラスロットにキャラカードがいない
  - 今ターンの戦闘前1でキャラカードをフィールドに出した（新キャラ配置ターン）
  - スキルカードの属性が相手キャラの**得意属性**（`AttributeDatabaseSO` で設定。`None` 以外）と一致する（耐性によりダメージ無効）

### ダメージ計算

- ATK 合計が相手スロットのキャラカードの**有効 DEF**（DEF + DefBoost）を**上回った場合**、その差分（ATK - 有効DEF）の枚数だけ相手デッキの上からカードを墓地へ送る
- ATK 合計が有効 DEF 以下の場合はダメージなし
- 相手スロットにキャラカードがいない場合、DEF = 0 として計算する
- 両プレイヤーのダメージを同時に計算する（先攻後攻なし）

---

## コストシステム

全カードは `Cost` 値を持つ。カードをプレイするとき、コストの枚数分だけ**自分のデッキ上から墓地**へカードを送る。

| カード種別 | コストを払うタイミング |
|---|---|
| キャラカード（キャラセット） | Resolve 後、カードが表向きになった直後 |
| キャラ・スキルカード（戦闘前1） | 戦闘フェーズで全フィールドカードが表向きになった直後 |
| イベントカード（戦闘前2） | OK ボタンで Ready を確定した直後 |

- コスト 0 のカードは墓地送りアニメーションをスキップする
- コスト > デッキ残枚数の場合は残り全枚数を払い切り、演出完了後に敗北判定
- コスト == デッキ残枚数の場合はデッキが 0 枚になるが敗北しない

---

## カード状態

| 状態 | 説明 |
|---|---|
| Normal | フィールド上で待機中 |
| Ready | 解決フェーズで処理される待機状態。対象はすでに決定済み（視覚的な区別なし） |
| Resolve | 解決フェーズで現在処理中のカード。黄色枠で表示 |

---

## 勝敗条件

以下のいずれかが発生した時点で敗北。

| 発生タイミング | 条件 |
|---|---|
| ドローフェーズ開始時 | 自分のデッキが 0 枚 |
| コスト支払い時（キャラセット・戦闘前1・戦闘前2・戦闘フェーズ） | 払うコストがデッキ残枚数より多い（コスト > デッキ残枚数）。演出で残り全枚数を払い切った後に敗北。コスト == デッキ残枚数の場合は敗北しない |
| Draw エフェクトで次の 1 枚を引こうとした時 | 自分のデッキが 0 枚 |
| 戦闘フェーズ・ダメージ計算後 | ダメージを受けて自分のデッキが 0 枚になった |

- 上記以外のタイミングでデッキが 0 枚になること自体は即座に敗北にならない
- 勝敗決定時に画面中央に「YOU WIN」「YOU LOSE」「DRAW」の演出を表示し、ホームに戻るボタンが出現する
